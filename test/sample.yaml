---
application: git
alias:
    contains: "tag --sort=creatordate --contains"
    push_gerrit: "push origin HEAD:refs/for/"
    st: "status"

aliasmate:
    verbose: True
    concatenate_symbols: "++"